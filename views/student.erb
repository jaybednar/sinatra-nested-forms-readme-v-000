<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Student</title>
  </head>
  <body>
    <div class="student">
      <h3>Name: <%= @student.name %> </h3>
      <h4>Grade: <%= @student.grade %> </h4>
    </div>

    <div class="course">
      <p>Name: <%= course.name %> </p>
      <p>Grade: <%= course.grade %> </p>
    </div>
  </body>
</html>
